---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
requirements:
  - DATA-01
  - DATA-02
files_modified:
  - src/data_collection/collect_bond_yields.py
  - src/data_collection/collect_macro_data.py
  - data/processed/bond_yields.csv
  - data/processed/macro_features.csv
autonomous: true
user_setup: []
must_haves:
  truths:
    - Both collection scripts run from the repo root using the project virtual environment.
    - Processed outputs are regenerated with a documented monthly date convention and current country coverage.
  artifacts:
    - path: src/data_collection/collect_bond_yields.py
      contains: "OUTPUT_PATH"
      min_lines: 60
    - path: src/data_collection/collect_macro_data.py
      contains: "OUTPUT_PATH"
      min_lines: 90
    - path: data/processed/bond_yields.csv
      contains: "country"
      min_lines: 100
    - path: data/processed/macro_features.csv
      contains: "hicp_inflation"
      min_lines: 100
  key_links:
    - from: src/data_collection/collect_macro_data.py
      to: data/processed/bond_yields.csv
      via: bond yields input path
      pattern: "BOND_YIELDS_PATH\\s*=\\s*[\"']data/processed/bond_yields\\.csv[\"']"
    - from: src/data_collection/collect_bond_yields.py
      to: data/processed/bond_yields.csv
      via: output path
      pattern: "OUTPUT_PATH\\s*=\\s*[\"']data/processed/bond_yields\\.csv[\"']"
---

# Phase 1 Plan 01: Stabilize Collection Scripts

## Objective

- **What:** Verify the existing collection scripts, harden them for repeatable execution, and regenerate the processed source files.
- **Why:** Phase 2 depends on stable, rerunnable source datasets rather than one-off artifacts from Session 1.
- **Output:** Hardened collection scripts plus freshly generated `bond_yields.csv` and `macro_features.csv`.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@CLAUDE.md

## Tasks

<task type="auto">
  <name>Audit and harden the bond yield collector for repeatable execution</name>
  <files>src/data_collection/collect_bond_yields.py</files>
  <read_first>
    src/data_collection/collect_bond_yields.py
    .planning/PROJECT.md
    .planning/phases/01-data-foundation/01-RESEARCH.md
    CLAUDE.md
  </read_first>
  <action>Update `src/data_collection/collect_bond_yields.py` so it explicitly creates the `data/processed` directory before writing output, keeps the monthly `date` column normalized consistently for downstream joins, preserves the country list `DE, FR, IT, ES, PT, GR`, keeps the output path at `data/processed/bond_yields.csv`, and enforces an ECB request delay of at least `1.0` second between country requests to respect the repo guidance about aggressive rate limiting.</action>
  <automated>
    <command>.\.venv\Scripts\python.exe -m py_compile src/data_collection/collect_bond_yields.py</command>
    <expects>Exit code 0</expects>
    <command>.\.venv\Scripts\python.exe src/data_collection/collect_bond_yields.py</command>
    <expects>`data/processed/bond_yields.csv` exists after execution</expects>
  </automated>
  <acceptance_criteria>
    - src/data_collection/collect_bond_yields.py contains "OUTPUT_PATH"
    - src/data_collection/collect_bond_yields.py contains "data/processed/bond_yields.csv"
    - src/data_collection/collect_bond_yields.py contains "COUNTRIES = [\"DE\", \"FR\", \"IT\", \"ES\", \"PT\", \"GR\"]"
    - src/data_collection/collect_bond_yields.py contains "REQUEST_DELAY = 1.0"
  </acceptance_criteria>
  <verify>Run `.\.venv\Scripts\python.exe -m py_compile src/data_collection/collect_bond_yields.py` and then `.\.venv\Scripts\python.exe src/data_collection/collect_bond_yields.py` from the repo root.</verify>
  <done>The bond yield collector compiles, runs from the repo root, respects a 1.0 second ECB delay, and rewrites `data/processed/bond_yields.csv` successfully.</done>
</task>

<task type="auto">
  <name>Audit and harden the macro collector against the regenerated bond output</name>
  <files>src/data_collection/collect_macro_data.py, data/processed/macro_features.csv</files>
  <read_first>
    src/data_collection/collect_macro_data.py
    data/processed/bond_yields.csv
    .planning/phases/01-data-foundation/01-RESEARCH.md
    CLAUDE.md
  </read_first>
  <action>Update `src/data_collection/collect_macro_data.py` so it explicitly creates the output directory, keeps `BOND_YIELDS_PATH` set to `data/processed/bond_yields.csv`, preserves the `GR` to `EL` mapping for Eurostat, preserves an ECB request delay of at least `1.0` second for ECB calls, and writes a stable `data/processed/macro_features.csv` with a documented monthly `date` key and `country` key. Preserve the current public-source approach and outer-merge behavior unless execution shows a blocking issue.</action>
  <automated>
    <command>.\.venv\Scripts\python.exe -m py_compile src/data_collection/collect_macro_data.py</command>
    <expects>Exit code 0</expects>
    <command>.\.venv\Scripts\python.exe src/data_collection/collect_macro_data.py</command>
    <expects>`data/processed/macro_features.csv` exists after execution</expects>
  </automated>
  <acceptance_criteria>
    - src/data_collection/collect_macro_data.py contains "BOND_YIELDS_PATH = \"data/processed/bond_yields.csv\""
    - src/data_collection/collect_macro_data.py contains "ECB_TO_EUROSTAT = {\"DE\": \"DE\", \"FR\": \"FR\", \"IT\": \"IT\", \"ES\": \"ES\", \"PT\": \"PT\", \"GR\": \"EL\"}"
    - src/data_collection/collect_macro_data.py contains "REQUEST_DELAY = 1.0"
    - data/processed/macro_features.csv contains "hicp_inflation"
  </acceptance_criteria>
  <verify>Run `.\.venv\Scripts\python.exe -m py_compile src/data_collection/collect_macro_data.py` and then `.\.venv\Scripts\python.exe src/data_collection/collect_macro_data.py` from the repo root.</verify>
  <done>The macro collector compiles, runs against the regenerated bond file, preserves a 1.0 second request delay, and rewrites `data/processed/macro_features.csv` successfully.</done>
</task>

## Verification

- [ ] `.\.venv\Scripts\python.exe -m py_compile src/data_collection/collect_bond_yields.py src/data_collection/collect_macro_data.py` exits successfully.
- [ ] `data/processed/bond_yields.csv` and `data/processed/macro_features.csv` both exist after execution.
- [ ] Both scripts print current date ranges and coverage summaries without crashing.

## Success Criteria

- [ ] Both collection scripts are runnable from the repo root without manual patching.
- [ ] Regenerated processed CSVs exist and reflect the current source code.
- [ ] The date and country keys are stable enough for downstream country-month joins.
