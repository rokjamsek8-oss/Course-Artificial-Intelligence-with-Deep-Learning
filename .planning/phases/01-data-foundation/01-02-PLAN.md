---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
requirements:
  - DATA-02
files_modified:
  - src/data_collection/build_phase1_panel.py
  - data/processed/canonical_country_month_panel.csv
  - .planning/phases/01-data-foundation/01-data-audit.md
  - .planning/phases/01-data-foundation/01-data-contract.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Phase 1 leaves behind a canonical non-DE country-month panel that downstream modeling can consume directly.
    - Phase 2 has a written contract for the canonical join keys, benchmark handling, and completeness cutoff.
  artifacts:
    - path: src/data_collection/build_phase1_panel.py
      contains: "canonical_country_month_panel.csv"
      min_lines: 70
    - path: data/processed/canonical_country_month_panel.csv
      contains: "country,date"
      min_lines: 100
    - path: .planning/phases/01-data-foundation/01-data-audit.md
      contains: "# Phase 1 Data Audit"
      min_lines: 20
    - path: .planning/phases/01-data-foundation/01-data-contract.md
      contains: "# Phase 1 Data Contract"
      min_lines: 20
  key_links:
    - from: src/data_collection/build_phase1_panel.py
      to: data/processed/bond_yields.csv
      via: processed bond input
      pattern: "bond_yields\\.csv"
    - from: src/data_collection/build_phase1_panel.py
      to: data/processed/macro_features.csv
      via: processed macro input
      pattern: "macro_features\\.csv"
    - from: src/data_collection/build_phase1_panel.py
      to: data/processed/canonical_country_month_panel.csv
      via: canonical panel output
      pattern: "canonical_country_month_panel\\.csv"
---

# Phase 1 Plan 02: Build The Canonical Phase 1 Panel And Contract

## Objective

- **What:** Create the canonical non-DE country-month panel, save an audit of that panel, and document the Phase 1 handoff contract.
- **Why:** Phase 2 needs a stable modeling-ready base artifact, not just raw collection outputs plus documentation.
- **Output:** `build_phase1_panel.py`, `canonical_country_month_panel.csv`, `01-data-audit.md`, and `01-data-contract.md`.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@data/processed/bond_yields.csv
@data/processed/macro_features.csv

## Tasks

<task type="auto">
  <name>Create the canonical non-DE country-month panel builder</name>
  <files>src/data_collection/build_phase1_panel.py, data/processed/canonical_country_month_panel.csv, .planning/phases/01-data-foundation/01-data-audit.md</files>
  <read_first>
    data/processed/bond_yields.csv
    data/processed/macro_features.csv
    .planning/phases/01-data-foundation/01-RESEARCH.md
    .planning/phases/01-data-foundation/01-VALIDATION.md
  </read_first>
  <action>Create `src/data_collection/build_phase1_panel.py` that reads `data/processed/bond_yields.csv` and `data/processed/macro_features.csv`, filters to the modeled non-DE countries `FR, IT, ES, PT, GR`, normalizes the monthly `date` key, enforces a stable completeness cutoff at `2025-12`, removes duplicate `date` plus `country` rows, and writes the canonical modeling-ready artifact to `data/processed/canonical_country_month_panel.csv`. In the same script, write `.planning/phases/01-data-foundation/01-data-audit.md` with the title `# Phase 1 Data Audit` summarizing row counts, date ranges, country coverage, duplicate-key status, missingness for the key analytical columns, and the chosen completeness cutoff.</action>
  <automated>
    <command>.\.venv\Scripts\python.exe src/data_collection/build_phase1_panel.py</command>
    <expects>`data/processed/canonical_country_month_panel.csv` and `.planning/phases/01-data-foundation/01-data-audit.md` both exist after execution</expects>
  </automated>
  <acceptance_criteria>
    - src/data_collection/build_phase1_panel.py contains "canonical_country_month_panel.csv"
    - src/data_collection/build_phase1_panel.py contains "2025-12"
    - data/processed/canonical_country_month_panel.csv contains "country,date"
    - .planning/phases/01-data-foundation/01-data-audit.md contains "# Phase 1 Data Audit"
  </acceptance_criteria>
  <verify>Run `.\.venv\Scripts\python.exe src/data_collection/build_phase1_panel.py` and then read `data/processed/canonical_country_month_panel.csv` plus `.planning/phases/01-data-foundation/01-data-audit.md`.</verify>
  <done>The canonical panel builder runs successfully, produces a duplicate-free non-DE monthly panel through the 2025-12 completeness cutoff, and writes the audit artifact.</done>
</task>

<task type="auto">
  <name>Write the canonical data contract for downstream feature engineering</name>
  <files>.planning/phases/01-data-foundation/01-data-contract.md</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    data/processed/canonical_country_month_panel.csv
    .planning/phases/01-data-foundation/01-data-audit.md
  </read_first>
  <action>Create `.planning/phases/01-data-foundation/01-data-contract.md` with the title `# Phase 1 Data Contract`. Document the canonical handoff artifact as `data/processed/canonical_country_month_panel.csv`, define the grain as one row per non-DE `country` and monthly `date`, explain that DE is a benchmark source but not a row entity in the canonical panel, list the included columns and the stable completeness cutoff of `2025-12`, and state that duplicate `date` plus `country` keys are not allowed.</action>
  <automated>
    <command>Get-Content .planning\phases\01-data-foundation\01-data-contract.md</command>
    <expects>Output contains `# Phase 1 Data Contract`, `canonical_country_month_panel.csv`, `2025-12`, `country`, and `date`</expects>
  </automated>
  <acceptance_criteria>
    - .planning/phases/01-data-foundation/01-data-contract.md contains "# Phase 1 Data Contract"
    - .planning/phases/01-data-foundation/01-data-contract.md contains "canonical_country_month_panel.csv"
    - .planning/phases/01-data-foundation/01-data-contract.md contains "2025-12"
    - .planning/phases/01-data-foundation/01-data-contract.md contains "country"
    - .planning/phases/01-data-foundation/01-data-contract.md contains "date"
  </acceptance_criteria>
  <verify>Read `.planning/phases/01-data-foundation/01-data-contract.md` and confirm the artifact path, canonical grain, completeness cutoff, benchmark handling, and duplicate-key rule are present.</verify>
  <done>The contract document gives Phase 2 an explicit, enforceable handoff artifact and interpretation rules.</done>
</task>

## Verification

- [ ] `.\.venv\Scripts\python.exe src/data_collection/build_phase1_panel.py` exits successfully.
- [ ] `data/processed/canonical_country_month_panel.csv` exists and contains the canonical non-DE monthly panel.
- [ ] `.planning/phases/01-data-foundation/01-data-audit.md` and `.planning/phases/01-data-foundation/01-data-contract.md` exist and reflect the same cutoff and grain.

## Success Criteria

- [ ] The project has a persistent canonical Phase 1 handoff artifact for downstream modeling.
- [ ] The canonical join keys, completeness cutoff, and benchmark-country behavior are written down for Phase 2.
- [ ] Phase 1 no longer relies on ambiguous raw merge outputs as the modeling-ready substrate.
